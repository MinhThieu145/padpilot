
namespace ChatVisual
{
    /// <summary>
    /// Define the different roles of the chat message (either an Assistant - the AI generated response, or a User - the message sent by users)
    /// </summary>
    public enum ChatMessageRole
    {
        User,
        Assistant,
    }
}
